import 'package:flutter/material.dart';
class TurkcheBooks extends StatelessWidget {
  static const routeName= '/TurkcheBooks';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Туркчо китептер'),),
      
    );
  }
}