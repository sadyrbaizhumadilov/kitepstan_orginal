import 'package:flutter/material.dart';
class OruschaBooks extends StatelessWidget {
  static const routeName= '/OruschaBooks';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Орусча китептер?'),),
      
    );
  }
}