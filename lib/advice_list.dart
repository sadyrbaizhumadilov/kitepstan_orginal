import 'package:flutter/material.dart';
class Advice extends StatelessWidget {
  static const routeName= '/advice_list';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Сунуштарыныз?'),),
      
    );
  }
}