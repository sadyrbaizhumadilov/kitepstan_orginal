import 'package:flutter/material.dart';

class KBayalinovBooks extends StatelessWidget { 
  static const routeName= '/KBayalinovBooks';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text('KBayalinovBooks'),
    ),
      
    );
  }
}