import 'package:flutter/material.dart';

class ZhusupBalasagynBooks extends StatelessWidget { 
  static const routeName= '/ZhusupBalasagynBooks';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text('ZhusupBalasagynBooks'),
    ),
      
    );
  }
}