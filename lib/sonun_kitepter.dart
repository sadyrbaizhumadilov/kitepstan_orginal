import 'package:flutter/material.dart';

class Like extends StatelessWidget {
  static const routeName= '/sonun_kitepter';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text('Жактыргандарым^^'),
    ),
      
    );
  }
}