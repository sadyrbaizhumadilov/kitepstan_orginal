import 'package:flutter/material.dart';

class AOsmonovBooks extends StatelessWidget { 
  static const routeName= '/AOsmonovBooks';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text('AOsmonovBooks'),
    ),
      
    );
  }
}