import 'package:flutter/material.dart';

class Library extends StatelessWidget { 
  static const routeName= '/kitepkanaam';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text('Китептерим'),
    ),
      
    );
  }
}