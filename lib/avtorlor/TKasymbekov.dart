import 'package:flutter/material.dart';

class TKasymbekovBooks extends StatelessWidget { 
  static const routeName= '/TKasymbekovBooks';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text('Т.Касымбеков'),
    ),
      
    );
  }
}