import 'package:flutter/material.dart';

class PredmettikBooks extends StatelessWidget { 
  static const routeName= '/PredmettikBooks';
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text('PredmettikBooks'),
    ),
      
    );
  }
}